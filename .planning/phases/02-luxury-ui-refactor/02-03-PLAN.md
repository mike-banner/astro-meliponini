---
phase: 02-luxury-ui-refactor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CartDrawer.tsx
autonomous: false
requirements: [3]

must_haves:
  truths:
    - "The cart drawer price and total read in the honey-gold accent, matching the rest of the site"
    - "The empty-cart state uses luxury micro-copy instead of a flat 'Votre panier est vide.'"
  artifacts:
    - path: "src/components/CartDrawer.tsx"
      provides: "Cart drawer refined to the luxury accent + micro-copy"
      contains: "accent-gold"
  key_links:
    - from: "CartDrawer.tsx"
      to: "--accent-gold token"
      via: "text-[hsl(var(--accent-gold))] arbitrary class"
      pattern: "accent-gold"
---

<objective>
Refine the cart drawer (CartDrawer.tsx) to complete the luxury system. The drawer is ALREADY largely
on-brand (uppercase, `tracking-widest`, `rounded-none`, token colors from Phase 01), so this is a
light finishing pass, not a rebuild: add the honey-gold accent to the price/total and upgrade the
empty-state copy. Pillars: Color, Copywriting.

Purpose: Requirement 3 (Luxury UI). The drawer is the last surface the user sees before checkout; a
flat black total and generic "empty" message undercut the premium feel established everywhere else.

Output: CartDrawer.tsx with gold price/total accents and luxury empty-state micro-copy.

CONSTRAINT: This is a UI-only refinement. Do NOT touch the nanostores/CoCart cart logic, the
add/update/remove handlers, the checkout redirect, or the `$cart` subscription — that is Phase 01
functionality and must remain byte-for-byte intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/00-UI-REVIEW.md
@PROJECT_RULES.md

<design_system>
Reuse the shared tokens (src/styles/global.css):
- Honey gold: `hsl(var(--accent-gold))` → in a React/Tailwind arbitrary class:
  `text-[hsl(var(--accent-gold))]`.
- The drawer already uses `uppercase tracking-widest` and `rounded-none` — keep that.
- Established micro-copy register: French, evocative, honey/rarity themed
  (cf. "Découvrir nos élixirs", "Explorer l'univers").

<interfaces>
From src/components/CartDrawer.tsx (do not change these):
- Reads `$cart` (nanostores atom) with `$cart.totals.count`, `$cart.totals.total`, and `$cart.items`.
- Per-item: `item.item_key`, `item.name`, `item.sku`, `item.quantity.value`, and a computed `price`.
- Uses `SheetContent/SheetHeader/SheetTitle/SheetFooter` from src/components/ui/sheet.tsx.
- Has `isCheckingOut` state driving the "Commander" button animation.
</interfaces>
</design_system>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gold price/total accents and luxury empty-state copy to CartDrawer.tsx</name>
  <files>src/components/CartDrawer.tsx</files>
  <read_first>
    - src/components/CartDrawer.tsx (the file being modified — read fully; note lines ~86-91 empty state, ~131 per-item price, ~148-161 footer total + Commander button)
    - src/styles/global.css (the --accent-gold token)
    - src/pages/index.astro (micro-copy register reference)
  </read_first>
  <action>
    JSX-class-only changes (no logic):
    - Per-item price (line ~131, `<span className="font-semibold">{price} €</span>`): add the gold
      accent → `className="font-semibold text-[hsl(var(--accent-gold))]"`.
    - Footer total value (the `{$cart.totals.total}` span at line ~149-151 region): give the amount
      the gold accent `text-[hsl(var(--accent-gold))]`; keep the "Total" label black uppercase.
    - Empty-state copy (line ~87): change `Votre panier est vide.` to
      `Votre écrin est encore vide.` (matches the rare/precious "élixir" register). Keep the same
      `text-muted-foreground uppercase tracking-widest text-xs` styling and the CTA button below it.
    Leave the "Commander" button, quantity +/- handlers, remove button, SKU line, and all
    `$cart` reads / store actions exactly as they are.
  </action>
  <verify>
    <automated>grep -q "accent-gold" src/components/CartDrawer.tsx &amp;&amp; grep -q "Votre écrin est encore vide" src/components/CartDrawer.tsx &amp;&amp; ! grep -q "Votre panier est vide" src/components/CartDrawer.tsx &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Per-item price and footer total spans include `text-[hsl(var(--accent-gold))]`
    - Empty-state text is `Votre écrin est encore vide.`; `Votre panier est vide.` no longer present
    - No change to `$cart` reads, store actions, checkout handler, or quantity/remove logic
    - `npm run build` completes without error
  </acceptance_criteria>
  <done>Cart drawer shows honey-gold price/total and luxury empty-state copy; all Phase 01 cart logic intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cart drawer refined: item prices and the cart total now render in honey gold, and the empty state reads "Votre écrin est encore vide." No cart functionality changed.</what-built>
  <how-to-verify>
    1. `npm run dev`, open a product page, add an item to the cart.
    2. Open the cart drawer — confirm the item price and the Total value are honey gold.
    3. Remove all items — confirm the empty state reads "Votre écrin est encore vide."
    4. Confirm add / quantity +/- / remove / Commander all still work exactly as before.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still looks off.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` produces static output with no errors.
- `grep -c "accent-gold" src/components/CartDrawer.tsx` returns >= 2.
- Cart add/update/remove/checkout behavior unchanged (verified in checkpoint).
</verification>

<success_criteria>
The cart drawer's price/total carry the honey-gold accent and the empty state uses luxury micro-copy,
completing Requirement 3 across the final pre-checkout surface without altering any Phase 01 cart logic.
</success_criteria>

<output>
Create `.planning/phases/02-luxury-ui-refactor/02-03-SUMMARY.md` when done.
</output>
